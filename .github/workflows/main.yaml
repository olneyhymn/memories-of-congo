# workflow to run ./site/ebook.sh on push to master. Requires python and pandoc
name: Generate eBook

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v7

    - name: Set up Python
      uses: actions/setup-python@v6
      with:
        python-version: 3.12

    - name: install bs4
      run: |
        pip install beautifulsoup4

    - name: Install pandoc
      run: |
        sudo apt-get install -y pandoc

    - name: Run ebook script
      working-directory: site
      run: |
        ./ebook.sh 
        
    - name: Upload artifact
      uses: actions/upload-artifact@v7
      with:
        name: book
        path: site/book.epub
